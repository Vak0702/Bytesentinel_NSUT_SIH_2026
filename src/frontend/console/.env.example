# Copy to `.env`. Vite only exposes variables prefixed with VITE_.

VITE_API_ORIGIN=http://localhost:5000

# Where to bounce an officer with no valid session. Dev = the Next.js landing
# page on :3000; production = /login on the same origin.
VITE_LOGIN_URL=http://localhost:3000/login
